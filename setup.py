from setuptools import setup, find_packages 
setup(name = 'routers', packages = find_packages())
setup(name = 'auth', packages = find_packages())
setup(name = 'models', packages = find_packages())
setup(name = 'schemas', packages = find_packages())
setup(name = 'connection', packages = find_packages())
setup(name = 'utils', packages = find_packages())